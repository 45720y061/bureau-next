import BureauWebsite from "@/components/BureauWebsite";

// Server Component (default) — delegates interactivity to a Client Component.
export default function Page() {
  return <BureauWebsite />;
}
